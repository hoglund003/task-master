<%= form_with url: "/task_records", method: :post, id: "new-form" do |f| %>
  <%= render "week" %>
  <%= f.select(:user, @users) %>
  <ul class="task-list">
    <% @tasks.each do |task| %>
      <li>
        <%= render "task_button", task: task %>
      </li>
    <% end %>
  </ul>
<% end %>