<table>
  <thead>
    <tr>
      <td>#</td>
      <td>Name</td>
      <td>Points</td>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td>
          <div class="small-avatar">
            <%= image_tag user.avatar_url %>
          </div>
        </td>
        <td><%= user.name %></td>
        <td><%= user.points %></td>
        <td><%= button_to "…", user, method: :get %></td>
      </tr>
    <% end %>
  </tbody>
</table>