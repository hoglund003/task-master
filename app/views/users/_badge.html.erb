<div class="badge <%= "#{badge_class}" %>">
  <h4><%= title %></h4>
  <a href="<%= user_path(user) %>">
    <div class="small-avatar">
      <%= image_tag user.avatar_url %>
      <h2><%= user.name %></h2>
      <h2><%= points %>p</h2>
    </div>
  </a>
</div>